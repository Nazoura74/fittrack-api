name: exo08-partie-01

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Build par ${{ github.actor }} sur ${{ github.head_ref }}"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test
        run: echo "Tests lancés après le build sur ${{ github.head_ref }}"
